import { COLORS } from "./colors";
import { FONTS, FONT_SIZES, FONT_WEIGHTS } from "./font";
import { LINE_HEIGHT, SPACE } from "./spacing";

export const theme = {
  colors: COLORS,
  fonts: FONTS,
  space: SPACE,
  lineHeight: LINE_HEIGHT,
  fontWeight: FONT_WEIGHTS,
  fontSize: FONT_SIZES,
};
